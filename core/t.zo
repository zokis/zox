$Equal(expected, actual, message) {
    ? (expected == actual) {
        print(".");
        true
    } : {
        println("");
        println("Fail: " + message);
        println("Expected: " + expected);
        println("Actual: " + actual);
        false
    }
}
