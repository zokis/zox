$ add(a, b) {
    a + b
}

$ factorial(n) {
    ? (n <= 1) { 1 } : { n * factorial(n - 1) }
}

$ apply(f, x) {
    f(x)
}

$ double(x) { x * 2 }

println(add(3, 4));
println(factorial(5));
println(factorial(10));
println(apply(double, 5));
